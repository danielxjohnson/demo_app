<%= form_for(@user_name) do |f| %>
  <% if @user_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_name.errors.count, "error") %> prohibited this user_name from being saved:</h2>

      <ul>
      <% @user_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
